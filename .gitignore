# Rust
/target/
core/target/
src-tauri/target/

# Node / SvelteKit
frontend/node_modules/
frontend/.svelte-kit/
frontend/build/

# Tauri
src-tauri/gen/
dist/

# OS
.DS_Store

# Demo (generated by demo/seed.py)
demo/*.db

# Stray exports — native save dialog defaults to src-tauri/ (its cwd
# under `cargo tauri dev`) when a result/response export isn't redirected
src-tauri/*.csv
src-tauri/*.tsv
src-tauri/*.json
!src-tauri/tauri.conf.json
!src-tauri/tauri.macos-appstore.conf.json
!src-tauri/capabilities/*.json

# Written at CI time by appstore.yml — never committed
src-tauri/AppStore.provisionprofile
